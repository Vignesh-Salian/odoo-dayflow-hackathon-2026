/**
 * OWNER: Prajwal (Person D) — Phase 7 UI polish
 *
 * PLACEHOLDER on `main`. Copy the full file from `reference/copy-from-here`:
 *   git show reference/copy-from-here:frontend/src/components/LoadingState.tsx > frontend/src/components/LoadingState.tsx
 */
/** TODO: copy full LoadingState from reference/copy-from-here */
export function LoadingState({ label = "Loading…" }: { label?: string }) {
  return <div data-placeholder="LoadingState">{label}</div>;
}
